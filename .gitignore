# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/

# Virtualenv
.venv/
venv/

# Secrets — NEVER commit
.env
.env.local
.env.*.local

# ML model cache (250MB+ — let users download on first run)
.hf_cache/
*.bin

# Built indexes (regenerate via precompute.py)
output/
output_ivf/
runs/

# Large test datasets (regenerate via generate_synthetic_data.py)
data/candidates.jsonl

# Demo data — keep the 1,000-candidate sample under demo/demo_data/ tracked,
# but ignore large ad-hoc datasets at the root.
!demo/demo_data/candidates.jsonl

# Backups
/data/backups/
*.tar.gz.enc

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*.swo
*~
